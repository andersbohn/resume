Resume Play application
=====================================

FIXME
 - reintroduce some way of having page breaks ?!??!?!
TODO:
 - move date formatting from domain classes (CvItem subclasses) to ResumeHtml-module
 - have sbt build the project (for heroku)
   web: target/universal/stage/bin/ExampleBootStrap -Dhttp.port=$PORT
 - make run on heroku
   web: target/universal/stage/bin/ExampleBootStrap -Dhttp.port=$PORT 
 - fix missing grey boxes (or the resulting bad alignement) in print on the left titles
 
npm install --save-dev @lingui/cli
npm install --save-dev @lingui/loader
npm install --save-dev @lingui/macro
npm install --save-dev babel-plugin-macros
npm install --save-dev @babel/core
npm install --save @lingui/react make-plural